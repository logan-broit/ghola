apiVersion: v1
kind: Service
metadata:
  name: {{ include "ch-web.fullname" . }}
  labels:
    {{- include "ch-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ch-web.selectorLabels" . | nindent 4 }}
