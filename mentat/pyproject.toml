[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mentat"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "torch>=2.4",
    "numpy>=1.26",
    "hdbscan>=0.8.40",
]

[project.optional-dependencies]
dev = ["pytest>=8", "pytest-asyncio>=0.24", "ruff>=0.7", "httpx>=0.27"]

[tool.hatch.build.targets.wheel]
packages = ["mentat"]
