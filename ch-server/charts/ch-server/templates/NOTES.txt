Chapterhouse API Server has been deployed.

  Release:   {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}
  Image:     {{ include "ch-server.image" . }}

Health check:
  kubectl port-forward svc/{{ include "ch-server.fullname" . }} -n {{ .Release.Namespace }} 8080:{{ .Values.service.port }}
  curl http://localhost:8080/health

MCP endpoint:
  http://{{ include "ch-server.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.port }}/mcp/stateless

{{- if .Values.virtualService.enabled }}
Istio VirtualService:
  Host: {{ .Values.virtualService.host }}
  Gateway: {{ .Values.virtualService.gateway }}
{{- end }}

{{- if .Values.serviceMonitor.enabled }}
Prometheus ServiceMonitor enabled — metrics at /metrics
{{- end }}
